matrix:
  include:
    - os: linux
      language: generic
      dist: trusty
      sudo: required
      services:
        - docker
      script:
        - make test-docker
    - os: osx
      env: ACTION=SwiftPM
    - os: osx
      env: ACTION=Carthage
    - os: osx
      env: ACTION=CocoaPods

language: objective-c
osx_image: xcode10.2
script:
- make test-$ACTION
